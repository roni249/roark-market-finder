from __future__ import annotations

import pandas as pd


def enrich_with_fred(df: pd.DataFrame, api_key: str | None = None) -> pd.DataFrame:
    """Placeholder for future county economic enrichment."""
    return df.copy()

